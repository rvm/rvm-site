.breadcrumbs
  %a{ :href => "/" }
    Documentation
  &nbsp;>&nbsp;
  %a{ :href => "/interpreters/" }
    Interpreters
  &nbsp;>&nbsp;
  %a{ :href => "/interpreters/rbx/" }
    Rubinius (rbx)
  %hr


%h1
  Rubinius
%p
  Rubinius is a ruby interpreter written in C++ using many advanced techniques and technologies such as LLVM, a "precise, compacting, generational garbage collector",
  a compatible C extension API to standard MRI Ruby, and many other awesome ideas that will give any language-nut a huge nerdon.

%h3
  Website
%p
  %a{:href => "http://rubini.us/", :target => "_blank"}
    http://rubini.us/

%h3
  Installing

%pre.code
  :preserve
    $ rvm install rbx

%p
  Configuration options for rbx-head can be passed after `--`.
  %br
  It is important to keep proper order of options; `enable-version` must be specified before `default-version`:

  %pre.code
    :preserve
      $ rvm install rbx -- --enable-version=1.9,1.8 --default-version=1.8

%p
  If you're installing rubinius on Ubuntu, and you find that makeinfo
  is missing, you can solve that by installing the texinfo package by:

%pre.code
  :preserve
     $ sudo apt-get install texinfo

