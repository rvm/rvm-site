.breadcrumbs
  %a{ :href => "/" }
    Documentation
  &nbsp;>&nbsp;
  %a{ :href => "/packages/" }
    Libraries
  &nbsp;>&nbsp;
  %a{ :href => "/packages/iconv/" }
    Iconv
  %hr

%h2
  Iconv

%h2
  require "iconv" # => false ?!
%p
  If you have issues with iconv not being available in ruby / irb please try the following configure option:
%pre.code
  :preserve
    ∴ rvm package install iconv
    ∴ rvm remove 1.9.1
    ∴ rvm install 1.9.1 -C --with-iconv-dir=$HOME/.rvm/usr
%p
  Where you replace '/usr/local' with the prefix where your iconv packages are. (Thanks to Justin Chen for bringing this doosey to our attention!)
%p
  If you simply cannot seem to get this to work you can have RVM install a copy of readline into ~/.rvm/usr like so:
  %br
  %pre.code
    :preserve
      ∴ rvm package install iconv
      ∴ rvm remove 1.9.1
      ∴ rvm install 1.9.1
